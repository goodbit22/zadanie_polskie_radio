apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: minio
  namespace: minio
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: minio
  minReplicas: 1
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 90